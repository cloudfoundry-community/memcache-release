set -e

. common/setup-java-prepackaging.sh

cd ${BUILD_DIR}/memcache-broker
mvn -B -e clean install -DskipTests=true
mv target/memcache-broker.jar ${BUILD_DIR}/

cd ${BUILD_DIR}
rm -rf memcache-broker

cleanup_java

